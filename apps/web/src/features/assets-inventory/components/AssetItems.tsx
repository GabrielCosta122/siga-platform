import { Check, MoreHorizontal } from 'lucide-react';import { Badge } from '@/components/ui/badge';import { Button } from '@/components/ui/button';import { Card,CardContent } from '@/components/ui/card';import type {AssetItem} from '@/features/assets-inventory/data';import {cn} from '@/lib/utils'
function AssetItems({items,selected,onSelect,mode}:{items:AssetItem[];selected:string;onSelect:(i:AssetItem)=>void;mode:'cards'|'list'}){return <div className={mode==='cards'?'grid gap-3 sm:grid-cols-2':'space-y-2'}>{items.map(i=>{const Icon=i.icon;const active=i.id===selected;return <Card key={i.id} className={cn('cursor-pointer shadow-book-xs',active&&'ring-2 ring-primary/30')}><CardContent className={cn('flex gap-3 pt-5',mode==='list'&&'items-center py-4')}><button type="button" onClick={()=>onSelect(i)} aria-pressed={active} className="flex min-w-0 flex-1 gap-3 text-left"><span className="flex size-11 shrink-0 items-center justify-center rounded-md bg-secondary text-primary"><Icon className="size-5" aria-hidden="true"/></span><span className="min-w-0"><span className="flex flex-wrap gap-2"><span title={i.name} className="truncate text-sm font-medium">{i.name}</span>{i.important?<Badge variant="secondary" className="gap-1"><Check aria-hidden="true"/>Importante</Badge>:null}</span><span className="mt-1 block text-xs text-muted-foreground">{i.room} · {i.category} · {i.date}</span><span className="mt-2 block text-sm font-semibold">{i.value}</span></span></button><div className="shrink-0 text-right"><Badge variant="outline">{i.status}</Badge><Button variant="ghost" size="icon" aria-label={`Opções para ${i.name}`}><MoreHorizontal aria-hidden="true"/></Button></div></CardContent></Card>})}</div>};export {AssetItems}
