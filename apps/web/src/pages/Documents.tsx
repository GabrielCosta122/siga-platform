import { useMemo,useState } from 'react'; import { FilePlus2,Files,FolderPlus,Search } from 'lucide-react'; import { Button } from '@/components/ui/button';import { Card,CardContent } from '@/components/ui/card';import { Input } from '@/components/ui/input';import { DocumentDetails } from '@/features/documents/components/DocumentDetails';import { DocumentList } from '@/features/documents/components/DocumentList';import { categories,documents,folders,Folder } from '@/features/documents/data'
function Documents(){const [search,setSearch]=useState('');const [category,setCategory]=useState('Todas');const [type,setType]=useState('Todos');const [important,setImportant]=useState(false);const [linked,setLinked]=useState(false);const [selected,setSelected]=useState(documents[0]);const filtered=useMemo(()=>documents.filter(d=>(!search||d.name.toLowerCase().includes(search.toLowerCase()))&&(category==='Todas'||d.category===category)&&(type==='Todos'||d.type===type)&&(!important||d.important)&&(!linked||d.chapter)),[search,category,type,important,linked]);const clear=()=>{setSearch('');setCategory('Todas');setType('Todos');setImportant(false);setLinked(false)};return <div className="bg-background px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1200px] space-y-8"><header className="flex flex-col gap-5 border-b pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-muted-foreground">12 documentos</p><h1 className="font-display text-3xl font-semibold tracking-tight sm:text-4xl">Documentos</h1><p className="mt-1 text-sm text-muted-foreground sm:text-base">Tudo o que registra e protege a história do seu lar.</p></div><div className="flex gap-2"><Button variant="outline"><FolderPlus aria-hidden="true"/>Criar pasta</Button><Button><FilePlus2 aria-hidden="true"/>Adicionar documento</Button></div></header><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[[Files,'12 documentos'],[Folder,'4 categorias'],[Files,'5 vinculados a capítulos'],[Files,'3 vinculados a movimentações']].map(([Icon,label])=>{const I=Icon as typeof Files;return <Card key={label as string} size="sm" className="shadow-book-xs"><CardContent className="flex min-h-[5rem] items-center justify-center gap-3"><I className="size-5 text-primary" aria-hidden="true"/><p className="text-sm font-medium">{label as string}</p></CardContent></Card>})}</section><section><h2 className="font-display text-xl font-semibold">Documentos importantes</h2><div className="mt-4 grid gap-3 lg:grid-cols-3">{documents.filter(d=>d.important).map(d=>{const Icon=d.icon;return <Card key={d.id} size="sm" className="shadow-book-xs"><CardContent className="flex gap-3"><Icon className="size-5 text-primary" aria-hidden="true"/><div className="min-w-0"><p className="truncate text-sm font-medium">{d.name}</p><p className="mt-1 text-xs text-muted-foreground">{d.category} · {d.date} · {d.size}</p><Button variant="ghost" size="sm" className="mt-2">Abrir</Button></div></CardContent></Card>})}</div></section><section className="flex flex-wrap gap-2">{categories.map(([name,count,Icon])=><Button key={name} variant={category===name?'secondary':'outline'} size="sm" onClick={()=>setCategory(category===name?'Todas':name)}><Icon aria-hidden="true"/>{name} ({count})</Button>)}</section><section className="space-y-4"><div className="flex flex-wrap gap-2"><div className="relative min-w-56 flex-1"><Search className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-muted-foreground" aria-hidden="true"/><Input className="pl-9" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar documento"/></div><select value={type} onChange={e=>setType(e.target.value)} className="h-8 rounded-lg border bg-card px-3 text-sm"><option>Todos</option><option>PDF</option><option>Imagem</option><option>Documento</option><option>Planilha</option></select><Button variant={important?'secondary':'outline'} size="sm" aria-pressed={important} onClick={()=>setImportant(!important)}>Importantes</Button><Button variant={linked?'secondary':'outline'} size="sm" aria-pressed={linked} onClick={()=>setLinked(!linked)}>Com capítulos</Button>{search||category!=='Todas'||type!=='Todos'||important||linked?<Button variant="ghost" size="sm" onClick={clear}>Limpar filtros</Button>:null}</div><div className="grid items-start gap-6 xl:grid-cols-[minmax(0,1fr)_19rem]">{filtered.length?<DocumentList items={filtered} selected={selected.id} onSelect={setSelected}/>:<Card><CardContent className="py-12 text-center"><h3 className="font-display text-xl font-semibold">Nenhum documento encontrado</h3><p className="mt-2 text-sm text-muted-foreground">Adicione um arquivo ou ajuste os filtros para encontrar o que procura.</p><div className="mt-5 flex justify-center gap-2"><Button>Adicionar documento</Button><Button variant="outline" onClick={clear}>Limpar filtros</Button></div></CardContent></Card>}<aside className="xl:sticky xl:top-20"><DocumentDetails item={selected}/></aside></div></section><section><h2 className="font-display text-xl font-semibold">Pastas da casa</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{folders.map(([name,count,date])=><Card key={name} size="sm" className="shadow-book-xs"><CardContent><Folder className="size-4 text-primary" aria-hidden="true"/><p className="mt-2 text-sm font-medium">{name}</p><p className="mt-1 text-xs text-muted-foreground">{count} documentos · {date}</p></CardContent></Card>)}</div></section><section className="rounded-lg bg-secondary/70 p-5 sm:flex sm:items-center sm:justify-between"><div><h2 className="font-display text-lg font-semibold">Documentos também fazem parte da história</h2><p className="mt-1 text-sm text-muted-foreground">Vincule arquivos a capítulos, pagamentos e momentos importantes para manter todo o contexto reunido.</p></div><Button variant="ghost" className="mt-3 sm:mt-0">Ver vínculos</Button></section></div></div>};export {Documents}
