import { useMemo, useState } from 'react'
import { Banknote, BookOpen, CalendarDays, Filter, Landmark, Plus, Search, WalletCards } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { CommitmentsList } from '@/features/financial/components/CommitmentsList'
import { FinancialSummaryCard } from '@/features/financial/components/FinancialSummaryCard'
import { InvestmentEvolution } from '@/features/financial/components/InvestmentEvolution'
import { MovementList } from '@/features/financial/components/MovementList'
import { distribution, movements } from '@/features/financial/data'

function Financial() {
  const [search, setSearch] = useState(''); const [category, setCategory] = useState('Todas'); const [status, setStatus] = useState('Todos')
  const filtered = useMemo(() => movements.filter((m) => (!search || m.title.toLowerCase().includes(search.toLowerCase())) && (category === 'Todas' || m.category === category) && (status === 'Todos' || m.status === status)), [search, category, status])
  return <div className="bg-background px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1200px] space-y-8"><header className="flex flex-col gap-5 border-b pb-6 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="font-display text-3xl font-semibold tracking-tight sm:text-4xl">Financeiro</h1><p className="mt-1 text-sm text-muted-foreground sm:text-base">Cada investimento ajuda a construir esta história.</p></div><div className="flex flex-wrap gap-2"><Button variant="outline"><CalendarDays aria-hidden="true" /> Ver compromissos</Button><Button><Plus aria-hidden="true" /> Registrar movimentação</Button></div></header><div className="flex items-center gap-2 text-sm text-muted-foreground"><Filter className="size-4" aria-hidden="true" /> Todos os períodos</div><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><FinancialSummaryCard icon={Landmark} value="R$ 42.850,00" label="Total investido" detail="na história do lar"/><FinancialSummaryCard icon={WalletCards} value="R$ 31.200,00" label="Pago até agora" detail="etapas já concluídas"/><FinancialSummaryCard icon={CalendarDays} value="R$ 11.650,00" label="Compromissos futuros" detail="próximas etapas"/><FinancialSummaryCard icon={Banknote} value="R$ 1.450,00" label="Próximo pagamento" detail="10 de agosto de 2026"/></section><section className="grid gap-5 xl:grid-cols-[minmax(0,1.1fr)_minmax(20rem,0.9fr)]"><InvestmentEvolution/><CommitmentsList/></section><section className="rounded-lg bg-secondary/70 p-5 sm:flex sm:items-center sm:justify-between sm:gap-6"><div><h2 className="font-display text-lg font-semibold">Finanças também contam histórias</h2><p className="mt-1 text-sm text-muted-foreground">Vincule uma movimentação a um capítulo para lembrar o que aquele investimento representou.</p></div><Button variant="ghost" className="mt-3 sm:mt-0"><BookOpen aria-hidden="true" /> Ver capítulos relacionados</Button></section><section className="space-y-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="font-display text-xl font-semibold">Movimentações recentes</h2><p className="mt-1 text-sm text-muted-foreground">Registros que acompanham a construção da casa.</p></div><span className="text-xs text-muted-foreground">{filtered.length} movimentações</span></div><div className="flex flex-wrap gap-2"><div className="relative min-w-52 flex-1"><Search className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-muted-foreground" aria-hidden="true"/><Input value={search} onChange={(e)=>setSearch(e.target.value)} className="pl-9" placeholder="Buscar movimentação"/></div><select value={category} onChange={(e)=>setCategory(e.target.value)} className="h-8 rounded-lg border bg-card px-3 text-sm"><option>Todas</option><option>Aquisição</option><option>Construção</option><option>Documentação</option><option>Reforma</option></select><select value={status} onChange={(e)=>setStatus(e.target.value)} className="h-8 rounded-lg border bg-card px-3 text-sm"><option>Todos</option><option>Pago</option><option>Pendente</option><option>Agendado</option><option>Cancelado</option></select></div>{filtered.length ? <MovementList movements={filtered}/>:<Card><CardContent className="py-12 text-center"><h3 className="font-display text-xl font-semibold">Ainda não há movimentações neste período</h3><p className="mt-2 text-sm text-muted-foreground">Registre um investimento, pagamento ou compromisso para começar.</p><Button className="mt-5"><Plus aria-hidden="true"/> Registrar movimentação</Button></CardContent></Card>}</section><section><h2 className="font-display text-xl font-semibold">Onde estamos investindo</h2><div className="mt-4 grid gap-3 lg:grid-cols-2">{distribution.map(({label,value,percent,icon:Icon})=><Card key={label} size="sm" className="shadow-book-xs"><CardContent className="flex items-center gap-3"><Icon className="size-4 text-primary" aria-hidden="true"/><div className="min-w-0 flex-1"><div className="flex justify-between gap-3 text-sm"><span className="font-medium">{label}</span><span>{value} · {percent}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-primary/70" style={{width:`${percent}%`}}/></div></div></CardContent></Card>)}</div></section></div></div>
}
export { Financial }
