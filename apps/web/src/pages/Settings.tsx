import{useState}from'react';import{CheckCircle2,Save,Server}from'lucide-react';import{Button}from'@/components/ui/button';import{Card,CardContent}from'@/components/ui/card';import{SettingsNav}from'@/features/settings/components/SettingsNav';import{SettingsPanel}from'@/features/settings/components/SettingsPanel'
function Settings(){const[active,setActive]=useState('property');const[state,setState]=useState<'saved'|'pending'|'saving'>('saved');const mark=()=>setState('pending');const save=()=>{setState('saving');setTimeout(()=>setState('saved'),700)};const label=state==='saved'?'Todas as alterações estão salvas':state==='pending'?'Há alterações pendentes':'Salvando alterações…';return <div className="bg-background px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1200px] space-y-8"><header className="flex flex-col gap-5 border-b pb-6 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="font-display text-3xl font-semibold tracking-tight sm:text-4xl">Configurações</h1><p className="mt-1 text-sm text-muted-foreground sm:text-base">Organize as bases que sustentam a história da sua casa.</p><p className="mt-3 flex items-center gap-1.5 text-xs text-success"><CheckCircle2 className="size-3.5"/>{label}</p></div><Button onClick={save} disabled={state==='saving'}><Save/>Salvar alterações</Button></header><div className="grid items-start gap-7 lg:grid-cols-[14rem_minmax(0,1fr)]"><aside className="lg:sticky lg:top-20"><SettingsNav active={active} onChange={setActive}/></aside><SettingsPanel active={active} onChange={mark}/></div><Card size="sm" className="shadow-book-xs"><CardContent className="flex flex-wrap items-center gap-3"><Server className="size-5 text-success"/><div><p className="text-sm font-medium">Status do SIGA</p><p className="text-xs text-muted-foreground">Sistema disponível · última sincronização agora · 2,4 GB de 10 GB · SIGA 0.1.0</p></div></CardContent></Card></div></div>};export{Settings}
